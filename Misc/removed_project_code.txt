seems to be getting rid of the piece and replacing it with 0 even though a piece is drawn there

Removed project code:

# if selectedPiece.getKing == True:
if self.gameBoard[destRow][destCol] == 0 and (([destRow] == [selectedPieceRow - 1]) or ([destRow] == [selectedPieceRow + 1])) and (([destCol] == [selectedPieceCol - 1]) or ([destCol] == [selectedPieceCol + 1])):
	selectedPiece.move(destRow, destCol)
        self.gameBoard[selectedPieceRow][selectedPieceCol], self.gameBoard[destRow][destCol] = self.gameBoard[destRow][destCol], self.gameBoard[selectedPieceRow][selectedPieceCol]
        return True


Might need this back in King move section:            
elif destRow == selectedPieceRow + 1 and selectedPiece.getKing() is not True:
	return False